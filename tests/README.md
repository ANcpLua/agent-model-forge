# Root Test Coordination

These directories are for cross-tree checks that are not owned by one runtime.

- `shared/` for runtime-neutral fixtures
- `claude/` for Claude-specific repo-wide checks
- `codex/` for Codex-specific repo-wide checks
- `parity/` for side-by-side equivalence checks

# Plugin Boundary

Plugins are optional. Core execution behavior should remain understandable without them.

Use this directory only for:

- local augmentation helpers
- event-driven checks
- external integration shims that cannot live in skills

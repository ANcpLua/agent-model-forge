# Source Merge Matrix

When to use:

- when combining ideas from several repositories
- when some upstream features are not meaningfully comparable

Required output:

1. source capability
2. local equivalent
3. retained or excluded status
4. reason

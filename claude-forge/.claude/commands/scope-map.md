# Scope Map

When to use:

- before touching more than one subsystem
- when a task mentions "merge", "refactor", or "unify"

Required output:

1. current surfaces
2. proposed surfaces
3. delta and risk

# Claude MCP Notes

This directory holds model-adjacent MCP configuration examples.

Rules:

- treat MCP as an explicit dependency
- keep server definitions small and local-first
- avoid hiding policy inside connector configuration

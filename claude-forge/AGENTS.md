# Claude Forge Scope

- Keep Claude-native artifacts in Markdown unless a stronger reason exists.
- Prefer role briefs that are easy to edit during active work.
- Put reusable work behavior in skills, not in a growing global memory file.
- Keep hooks narrow: enforce gates, do not hide logic.
- Treat `.claude/generated/` as generated runtime output, not the primary authoring surface.

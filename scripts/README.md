# Root Scripts

- `lint-all.sh` checks the repo-level markers
- `validate-all.sh` runs narrow validation scripts from each subtree
- `sync-all.sh` runs the unified adapters
- `run-smoke-all.sh` runs small behavioral smoke checks

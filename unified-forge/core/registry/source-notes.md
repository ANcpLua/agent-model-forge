# Source Notes

This registry tracks capability equivalence, not external project lineage.

Rules:

- do not copy upstream naming just because it is popular
- prefer explicit capability descriptions over marketplace-style labels
- mark non-comparable features in the top-level reference matrix
